본 프로젝트는 지자기 값을 통해 사용자가 특정 위치에 존재하는지 특정하는 것이 주요 문제입니다.
사용할 데이터는 사용자가 걸을 수 있는 하나의 경로, 또는 최대한 긴 형태도 걸어서 측정한 경로 데이터 데이터 들이 모여있으며 "law_data"에 존재하는 데이터들로
각 scv의 이름들은 {from}_{to}_{count}.scv의 형태로 되어있습니다.
측정은 모든 노드 중 일정거리를 두고 마커라는 새로운 단위로써 측정 편의상 만든 단위입니다.
각 노드의 정확한 지점과 측정에 도움이 되는 두 값 hightrighted와 rightangle에 대해서도 설명이 필요한데 이와 관련된 정보는 추후 기술하겠습니다.
Liquid AI사의 액체 신경망 중 하나인 Hyena Edge라는 아키텍처를 기반으로 모델학습을 할 예정이다.
위의 아키텍처는 전체 경로에 대한 학습에 대해서 기억을 잘한다는 특성을 가졌다고 하여서 한번에 긴 경로를 입력으로 사용한다면 모델 학습에 큰 장점이 있을듯 합니다. 거기에 실제 사용시에는 윈도우 크기를 250정도로 줄여서 추론도 가능하다고 알고있습니다.
긴 시퀀스의 구조·패턴을 통째로 학습
trajectory 전체 패턴(지문 형태)을 내재적으로 기억
orientation 변화가 섞여 있는 구간도 long-conv가 전체 문맥을 잡아 안정화
긴 필터(long conv) 자체가 low-frequency 구조적 smoothing 효과

모델의 입력값으로는 자기장 벡터(x,y,z)만 쓰는것이 1차 목표이고 추가로 사용할 입력으로는 방향벡터 (yaw,pitch,roll)이 있습니다.
출력값으로는 특정 위치 x,y로 나왔으면 좋겠습니다.

최종적으로 원하는 모델은 이겁니다. 임위의 위치에 있는 사용자가 다섯걸음 걸어가면 그 때부터 사용자의 위치를 지속적으로 추정해주는 모델을 원합니다. 모델 학습간 원하는 모델 성능 입니다. 
- PE : 각 타임스텝에서의 거리 오차
- MAE : 전체 시퀀스 평균 위치 오차
- RMSE : 큰 오차에 더 민감
- 90% error
- ADE : 전체 경로가 얼마나 맞는지
- FDE : 하나의 trajectory 전체에 대한 평균 위치 오차
- DTW Distance : 예측 경로와 GT 경로가 시간축이 약간 어긋나도 “형태가 비슷한지”를 보고 싶을 때
- Jitter : 프레임 간 위치 변화 분산 - 현실 테스트 필요
- Noise Robustness : 인위적 노이즈 추가 후, 성능 저하율 측정

 이러한 결과물이 나올 수 있도록 데이터 전처리, 모델, 평가 3단계를 구상하여야 하며 특히 평가 단계에서는 

 - PE : 각 타임스텝에서의 거리 오차
- MAE : 전체 시퀀스 평균 위치 오차
- RMSE : 큰 오차에 더 민감
- 90% error
- ADE : 전체 경로가 얼마나 맞는지
- FDE : 하나의 trajectory 전체에 대한 평균 위치 오차
- DTW Distance : 예측 경로와 GT 경로가 시간축이 약간 어긋나도 “형태가 비슷한지”를 보고 싶을 때
- Jitter : 프레임 간 위치 변화 분산 - 현실 테스트 필요
- Noise Robustness : 인위적 노이즈 추가 후, 성능 저하율 측정
를 시각적으로 볼 수 있도록 그래프 및 표로 정리해 주는 코드도 필요함
