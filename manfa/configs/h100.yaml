seed: 42
device: null

data:
  root: flow_matching/processed_data_flow_matching
  split: train
  window_size: 250
  stride: 50
  variance_thresh: 5e-4
  yaw_flip_thresh_deg: 30.0
  arc_over_net_tol: 2.0

field:
  hidden_dim: 256
  num_layers: 4
  fourier_features: 32
  dropout: 0.1
  output_variance: true

encoder:
  d_model: 320
  num_layers: 5
  d_state: 128
  d_conv: 4
  expand: 2
  dropout: 0.1

flow:
  num_particles: 96
  num_steps: 4
  noise_std: 0.08
  entropy_reg: 0.02

optim:
  lr: 0.0002
  weight_decay: 1.0e-5
  batch_size: 48
  epochs: 60
  grad_clip: 1.0

loss:
  lambda_field: 1.0
  lambda_contrastive: 0.4
  lambda_particle: 0.7
